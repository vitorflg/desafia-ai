query getChallenge($id: String!, $currentUserId: String) {
  challenge(id: $id, currentUserId: $currentUserId) {
    id
    details
    userGoogleId
    name
    description
    imageUrl
    categories
    tags
    acceptedByCurrentUser
  }
}
